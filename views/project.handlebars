<div class="text-center">
  <h2>{{name}}</h2>
  {{!-- <p>{{{get_emoji}}}</p> --}}
  <p>{{departure_date}}</p>
  <p>Projected by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="footprint">🦶</span>
    <span class="carbon-footprint fa-solid fa-shoe-prints">{{format_amount carbon_footprint}}</span>
    needed
  </p>
</div>
